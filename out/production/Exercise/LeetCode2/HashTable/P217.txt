public class P217 {
    public boolean containsDuplicate(int[] nums) {
        Set<Integer> hs = new HashSet<>();
        for (int k : nums) {
            if (hs.contains(k))
                return true;
            else
                hs.add(k);
        }
        return false;
    }
}